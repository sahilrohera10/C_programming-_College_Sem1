#include <stdio.h>
int main() {

int side;
scanf("%d" , &side);

int vol = side*side*side;
int area = 6*side*side;
printf("surface area is %d \n", area);
printf("volume is %d \n", vol);



    return 0;
}
